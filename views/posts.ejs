<!DOCTYPE html>
<html>
<head>
  <title>Express MongoDB App - Posts</title>
</head>
<body>
  <h1>Posts</h1>
  <% if (posts.length === 0) { %>
    <p>No posts available.</p>
  <% } else { %>
    <ul>
      <% posts.forEach(post => { %>
        <li>
          <h2><%= post.title %></h2>
          <p><%= post.content %></p>
        </li>
      <% }); %>
    </ul>
  <% } %>
</body>
</html>
